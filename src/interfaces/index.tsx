export interface ILabelProps {
    id: string,
    labelTitle: string, 
    inputPlaceholder: string,
}

export interface IAnchor {
    anchorHref: string,
    anchorName: string,
    picSrc: string,
    picAlt: string
}